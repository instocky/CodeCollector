# 📁 CodeCollector - Сборщик кода в один файл

Мощный Python скрипт для сбора всех файлов проекта в один текстовый файл с поддержкой интерактивного выбора, иерархической структуры папок и умной фильтрации.

## 🚀 Возможности

- ✅ **Автоматическая фильтрация** - исключает служебные папки и файлы
- ✅ **Поддержка .gitignore** - автоматически исключает файлы по паттернам
- ✅ **Интерактивный выбор** - красивый интерфейс с галочками
- ✅ **Иерархическое дерево** - навигация по папкам и файлам
- ✅ **Сортировка по времени** - новые файлы сверху
- ✅ **Поиск файлов** - быстрый поиск по имени
- ✅ **Кроссплатформенность** - работает на Windows, Linux, Mac
- ✅ **Память проекта** - запоминает выбор файлов и настройки
- ✅ **Markdown формат** - красивый вывод с подсветкой синтаксиса
- ✅ **Структура проекта** - генерация древовидной схемы

## 📋 Требования

- Python 3.6+
- Стандартные библиотеки (pathlib, fnmatch, termios/msvcrt, json)

## 🛠 Установка

1. Скачайте CodeCollector (`main.py`)
2. Сделайте его исполняемым (Linux/Mac):
   ```bash
   chmod +x main.py
   ```

## 💻 Использование

### Базовые команды

```bash
# Простой запуск CodeCollector
python main.py

# Указать директорию и выходной файл
python main.py /path/to/project output.txt

# Интерактивный режим
python main.py -i

# Сортировка по времени изменения
python main.py -t

# Markdown формат с структурой проекта
python main.py -m -s

# Комбинация флагов
python main.py -i -t -m -s /path/to/project collected_code.md
```

### Флаги командной строки

| Флаг                          | Описание                        |
| ----------------------------- | ------------------------------- |
| `-i`, `--interactive`         | Интерактивный выбор файлов      |
| `-t`, `--time`, `--sort-time` | Сортировка по времени изменения |
| `-m`, `--markdown`            | Markdown формат с подсветкой    |
| `-s`, `--structure`           | Включить структуру проекта      |
| `--no-time`                   | Отключить сортировку по времени |
| `--no-markdown`               | Отключить Markdown формат       |
| `--no-structure`              | Отключить структуру проекта     |

## 🧠 SaveSettings - Память проекта

CodeCollector автоматически запоминает ваши настройки и выбор файлов для каждого проекта!

### Как работает
- **Первый запуск**: выберите файлы и настройки → они сохраняются автоматически
- **Повторный запуск**: настройки и выбор восстанавливаются автоматически
- **Переопределение**: используйте негативные флаги для изменения сохраненных настроек

### Примеры работы с памятью

```bash
# Первый запуск проекта
python main.py -i -m -s /path/to/laravel-project
# 💾 Настройки проекта сохранены

# Повторный запуск - автоматически применяются сохраненные настройки
python main.py /path/to/laravel-project
# 📋 Применяемые флаги: -m -s (сохраненные настройки)

# Переопределение сохраненных настроек
python main.py --no-markdown /path/to/laravel-project
# 📋 Применяемые флаги: -s (отменен -m через --no-markdown)
```

### Что сохраняется
- ✅ Выбранные файлы и папки
- ✅ Флаги форматирования (`-m`, `-s`, `-t`)
- ✅ Имя выходного файла по умолчанию
- ❌ Интерактивный режим (не сохраняется)

### Файлы настроек
```
project_root/
├── .codecollector/
│   └── {project_name}.json    # Настройки проекта
├── .gitignore                 # Автоматически обновляется
└── ...
```

## 🎮 Управление в интерактивном режиме

### Навигация
- **↑↓** / **W/S** / **J/K** - навигация по списку
- **→** - развернуть папку
- **←** - свернуть папку
- **SPACE** - выбрать/отменить файл или папку

### Быстрые действия
- **A** - выбрать все файлы
- **N** - снять все выборы
- **+** - развернуть все папки
- **-** - свернуть все папки
- **F** - поиск файла по имени

### Завершение
- **ENTER** - подтвердить выбор и продолжить
- **Q** / **ESC** - выйти без сохранения

## 📁 Что исключается автоматически

### Служебные папки
- `vendor/` (Composer)
- `node_modules/` (npm)
- `venv/`, `env/` (Python virtual env)
- `.git/` (Git)
- `.vscode/` (VS Code)
- `__pycache__/` (Python cache)

### Служебные файлы
- `.env*` (переменные окружения)
- `*.pyc`, `*.pyo` (Python cache)
- `*.log` (логи)
- `.DS_Store` (macOS)
- `.gitignore`

### Файлы из .gitignore
Скрипт автоматически читает `.gitignore` и исключает все указанные в нем паттерны.

## 📊 Интерфейс CodeCollector

### Обычный режим
```
📋 Применяемые флаги: -m -s (сохраненные настройки)
CodeCollector: Сканирование директории /home/user/project
Загружено паттернов из .gitignore: 8
Сортировка по имени файла...
Найдено файлов: 45
Готово! Результат сохранен в Markdown формате со структурой: output.md
```

### С загрузкой настроек
```
🔄 Загружены настройки проекта 'laravel-app'
📋 Применяемые флаги: -t -m -s (сохраненные настройки)
CodeCollector: Сканирование директории...
💾 Настройки проекта сохранены
```

### Интерактивный режим
```
╔════════════════════════════════════════════════════════════════════════════════╗
║  ВЫБОР ФАЙЛОВ И ПАПОК (15/89 файлов выбрано)                                  ║
║  Страница 1/5 | Сортировка: по времени (новые ↑)                              ║
╠════════════════════════════════════════════════════════════════════════════════╣
║  ↑↓ - навигация, SPACE - выбор, →← - развернуть/свернуть                       ║
║  A/N - всё/ничего, +/- - развернуть/свернуть все, F - поиск, Q - выход        ║
╚════════════════════════════════════════════════════════════════════════════════╝

→ 📁 ☑ src/ (12 файлов) [28.05 14:32]
  📁 ◑ ▼ controllers/ (8 файлов)
    📄 ☑   UserController.php [28.05 15:20]
    📄 ☐   ProductController.php [28.05 13:45]
  📁 ☐ ▶ models/ (4 файла)
  📄 ☑ config.php [27.05 18:45]

Выбрано: 15 файлов
Текущий: src/controllers/UserController.php
```

## 📝 Форматы вывода

### Обычный текстовый формат
```
# src/main.py
#!/usr/bin/env python3
import os
from pathlib import Path

def main():
    print("Hello World")

# config/settings.php
<?php
return [
    'app_name' => 'My App',
    'debug' => true
];
```

### Markdown формат с подсветкой синтаксиса
```markdown
# CodeCollector - laravel-app

**Собрано файлов:** 23  
**Дата сбора:** 29.05.2025 15:30:45  
**Путь:** `/home/user/laravel-app`

## 📁 Структура проекта

```
laravel-app/
├── app/
│   ├── Http/
│   │   └── Controllers/
│   │       └── UserController.php
│   └── Models/
│       └── User.php
└── config/
    └── app.php
```

---

## 📄 Содержимое файлов

### `app/Http/Controllers/UserController.php`

```php
<?php
namespace App\Http\Controllers;

class UserController extends Controller
{
    public function index() {
        return response()->json(['users' => User::all()]);
    }
}
```
```

## 🎯 Типичные сценарии использования

### Laravel проект
```bash
# Первый запуск - настройка проекта
python main.py -i -m -s /path/to/laravel-project laravel_code.md

# Последующие запуски - автоматическое применение настроек
python main.py /path/to/laravel-project
```

### Анализ кодовой базы с историей изменений
```bash
python main.py -t -m /path/to/project analysis.md
```
- Получите весь код с сортировкой по времени изменения
- Увидите последние изменения сверху
- Красивый Markdown формат для чтения

### Подготовка для AI с памятью выбора
```bash
# Первый раз - выберите нужные файлы
python main.py -i /path/to/project code_for_ai.txt

# В следующий раз - тот же выбор применится автоматически
python main.py /path/to/project
```

## 🔧 Технические детали

### Поддерживаемые типы файлов
- Исходный код: `.py`, `.php`, `.js`, `.jsx`, `.ts`, `.tsx`, `.html`, `.css`, `.scss`, `.sql`
- Конфигурация: `.json`, `.xml`, `.yml`, `.yaml`, `.ini`, `.conf`
- Документация: `.md`, `.txt`
- И многие другие текстовые форматы

### Определение текстовых файлов
CodeCollector проверяет расширения файлов и содержимое (отсутствие нулевых байтов) для определения текстовых файлов.

### Обработка кодировок
- Приоритет: UTF-8
- Fallback: CP1251 (для русских файлов)
- При ошибке: пропуск с уведомлением

### Приоритет настроек
```
CLI флаги > Сохраненные настройки > Значения по умолчанию
```

## 🐛 Устранение неполадок

### Проблемы со стрелками
Если стрелки не работают, используйте альтернативы:
- **W/S** вместо ↑↓
- **J/K** (как в Vim)

### Ошибки кодировки
```
[Ошибка чтения файла: неподдерживаемая кодировка]
```
Файл будет пропущен, но обработка продолжится.

### Пустой результат
Проверьте:
- Правильность пути к проекту
- Наличие текстовых файлов
- Настройки .gitignore (возможно, исключает слишком много)

### Проблемы с настройками проекта
- Настройки хранятся в `.codecollector/{project_name}.json`
- При проблемах удалите папку `.codecollector` для сброса
- Папка автоматически добавляется в `.gitignore`

## 📈 Примеры использования

### Быстрый сбор PHP проекта с запоминанием
```bash
# Первый раз - настраиваем
python main.py -i -m -s ~/projects/my-php-app/ php_code.md

# Потом просто запускаем
python main.py ~/projects/my-php-app/
```

### React приложение с изменением настроек
```bash
# Изначально с Markdown
python main.py -i -m ~/projects/react-app/

# Потом без Markdown, но с сохранением выбора файлов
python main.py --no-markdown ~/projects/react-app/
```

### Анализ Python пакета
```bash
python main.py -i -t -m -s ~/projects/my-package/ package_analysis.md
```

## 🗺️ Roadmap

### ✅ Completed
- [x] **SaveSettings - Project Memory** - Remember file selections per project
  - ✅ Automatic settings persistence in `.codecollector/{project_name}.json`
  - ✅ Restore file selections and preferences between sessions
  - ✅ CLI argument priority system with negative flags support
  - ✅ Smart file filtering for deleted/moved files
  - ✅ Auto .gitignore management

### 🔥 High Priority
- [ ] **ConsoleSyntaxHighlight - Live Preview** - Реализовать подсветку синтаксиса в консоли для предпросмотра файлов (например, с использованием `rich` или `Pygments`). Предпросмотр должен появляться в панели под списком файлов при нажатии клавиши (например, 'V'), как в [обсуждаемом визуальном варианте](ссылка_на_обсуждение_или_скриншот_если_есть).

### 🎯 Medium Priority
- [ ] **Export Templates** - Настраиваемые шаблоны вывода
- [ ] **File Preview** - Предпросмотр содержимого файлов в интерактивном режиме
- [ ] **Statistics Dashboard** - Статистика по языкам, размерам файлов, изменениям

### 💡 Ideas
- [ ] **Plugin System** - Система плагинов для расширения функциональности
- [ ] **Web Interface** - Веб-интерфейс для удаленного использования
- [ ] **Git Integration** - Интеграция с Git для анализа изменений

## 🤝 Поддержка

Если возникли вопросы или предложения по улучшению CodeCollector, создайте issue или пишите разработчику.

## 📄 Лицензия

MIT License - используйте CodeCollector свободно для любых целей.

---
*CodeCollector создан с ❤️ для упрощения работы с кодом*